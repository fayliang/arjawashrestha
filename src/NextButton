import React from 'react';
import './Button.css';
 
const NextButton = ({ text, effectType }) => {
  return (
    <button style={{ margin: "10px" }} className={`custom-button ${effectType}`}>
      {text}
    </button>
  );
};
 
export default NextButton;